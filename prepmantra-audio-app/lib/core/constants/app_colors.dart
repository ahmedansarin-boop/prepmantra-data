import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF4F8EF7);        // Vibrant blue — interactive elements
  static const Color accent = Color(0xFF7C5CFC);         // Purple accent — highlights
  static const Color background = Color(0xFF0F0F1E);     // Deep navy — scaffold/page
  static const Color surface = Color(0xFF1A1A2E);        // Dark surface — cards, tiles
  static const Color surfaceVariant = Color(0xFF252540); // Slightly lighter — elevated cards
  static const Color onBackground = Color(0xFFE8E8F0);   // Primary text on dark bg
  static const Color onSurface = Color(0xFFB0B0C8);      // Secondary text
  static const Color divider = Color(0xFF2A2A45);        // Dividers
  static const Color error = Color(0xFFCF6679);          // Error red
  static const Color success = Color(0xFF4CAF50);        // Download complete
  static const Color favorite = Color(0xFFEF5350);       // Heart icon
}
